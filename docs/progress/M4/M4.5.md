# M4.5 Compare TypeScript gamma to Python (Dual-Engine Parity Test Suite)

Status: COMPLETE  
Owner: Antigravity  
Updated: 2026-08-26

Evidence:

- Implemented large-scale dual-engine cross-validation harness between TypeScript (`packages/options-engine`) and Python reference implementation (`tools/reference-python/black_scholes.py` and `gamma_reference.py`).
- Implemented high-throughput Python binary evaluator stream in `tools/reference-python/parity_evaluator.py`.
- Created TypeScript verification script `scripts/verify-parity.ts` and npm commands `npm run verify:parity` / `npm run test:parity`.
- Created Vitest automated parity test suite `tests/parity/dual-engine-parity.test.ts` covering 100,000 grid and randomized parameter combinations:
  - $S \in [10^3, 2 \times 10^5]$
  - $K \in [10^3, 3 \times 10^5]$
  - $T \in [1/365, 2.0]$
  - $\sigma \in [0.05, 3.0]$
  - $r = 0.0$
  - Calls and Puts
  - $\text{OI} \in [0.01, 10,000]\text{ BTC}$
- Evaluated 100,000 vectors with 100% pass rate (0 failures) against strict $\le 10^{-7}$ absolute and relative error bounds:
  - **Standard Normal CDF**: Max absolute $\Delta = 7.4517 \times 10^{-8} \le 10^{-7}$ (provably bounded by Abramowitz & Stegun error limit $\le 7.5 \times 10^{-8}$).
  - **Standard Normal PDF**: Max absolute $\Delta = 1.6653 \times 10^{-16} \le 10^{-7}$ (exact float64 precision).
  - **Black-Scholes $d_1$ / $d_2$**: Max absolute $\Delta = 5.6843 \times 10^{-14} \le 10^{-7}$.
  - **Deribit Inverse Gamma ($\Gamma$)**: Max absolute $\Delta = 2.7756 \times 10^{-17} \le 10^{-7}$, Max relative $\Delta = 2.2762 \times 10^{-13} \le 10^{-7}$.
  - **Modeled Signed GEX (USD 1% move)**: Max absolute $\Delta = 4.4703 \times 10^{-8} \le 10^{-7}$, Max relative $\Delta = 2.2765 \times 10^{-13} \le 10^{-7}$.
- Canonical benchmark vectors and edge cases (ATM, deep ITM/OTM wings, boundary $T=1/365$, $\sigma=3.0$) verified to machine precision.
