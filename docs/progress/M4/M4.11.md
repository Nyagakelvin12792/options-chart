# M4.11 Create known-output regression snapshots

Status: COMPLETE
Owner: Antigravity
Updated: 2026-08-26

Evidence:

1. **Golden Snapshot Generator & Fixture Suite**:
   - Implemented `tools/golden-generator.ts` producing deterministic, versioned golden fixtures representing four diverse market regimes in `tests/fixtures/golden/`:
     a) `normal-market.json`: Balanced calls/puts across 4 expiries (1D, 7D, 30D, 90D) and 9 strikes ($60k–$100k) with realistic IV smile.
     b) `volatility-crush.json`: Extreme IV spikes up to 330% (0.60 to 3.30 decimal IV) and severe skew under heavy open interest (36,000 BTC OI).
     c) `zero-dte.json`: Near-expiry 0DTE contracts exercising the 15-minute gamma profile floor (`minimumProfileTimeToExpiryMs`), verifying exclusion of <15m contracts from gamma profile while retaining 100% inclusion in Open Interest and Max Pain.
     d) `strike-skew.json`: Extreme strike range ($20,000 to $300,000) with massive wing open interest (8,000 BTC put OI at $40,000; 6,500 BTC call OI at $200,000) verifying asymptotic stability and non-NaN profile evaluation.

2. **Full Engine Output Computation & Golden Locking**:
   - Each snapshot computes the full options engine output via `calculateOptionsMetrics`:
     - Total Open Interest, Call OI, Put OI, Put/Call OI ratio.
     - Open Interest-weighted average Mark IV.
     - Total modeled GEX per 1% spot move.
     - Call Wall, Put Wall, and Secondary GEX level rankings.
     - Modeled Gamma Flip headline price and qualifying zero-crossings with significance thresholds.
     - Max Pain price and total payout for designated expiry.
     - Complete Gamma Profile spot grid.
     - Comprehensive calculation metadata and audit trail.

3. **SHA-256 Integrity Verification & Version Pinning**:
   - Stored golden fixture outputs are cryptographically hashed using SHA-256 over canonical alphabetically-sorted JSON (`canonicalJsonStringify`).
   - `ENGINE_VERSION = "0.1.0"` is verified across all snapshot fixtures and metadata.
   - Test asserts:
     - Anti-tamper verification of stored golden outputs against recorded SHA-256 hashes.
     - Exact bit-for-bit SHA-256 parity of freshly computed engine outputs against golden hashes.
     - Deep deterministic equality (`toEqual`) of all engine output structures.
     - Engine execution idempotence over 10 consecutive iterations.
     - Immediate detection of simulated bit flips and floating-point mutations.

4. **Automated Vitest Execution**:
   - Test suite implemented in `tests/snapshots/golden-options-calculations.test.ts` (12 tests passing, 0 failures).
   - Executed via `npx vitest run tests/snapshots/golden-options-calculations.test.ts`.
