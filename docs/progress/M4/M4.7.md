# M4.7 Set tolerances only after M4.6

Status: COMPLETE  
Owner: Antigravity  
Updated: 2026-08-26

Evidence: Comprehensive empirical calibration and automated tolerance verification completed:

1. **Documented Error Tolerances** (`docs/research/error-tolerances.md`):
   - **Black-Scholes Primitives ($d_1, d_2$, CDF, PDF)**: $\epsilon_{\text{rel}} \le 10^{-12}$ against closed-form analytical equations.
   - **Option Gamma ($\Gamma_{\text{BTC}}$) and GEX ($GEX_{1\%}$)**: $\epsilon_{\text{rel}} \le 10^{-7}$ against independent Python reference model (`tools/reference-python/gamma_reference.py`).
   - **Deribit Published JSON-RPC Mark IV / Greeks**: $\epsilon_{\text{rel}} \le 10^{-4}$ ($1.0\text{ bps}$) accounting for exchange 4-5 digit display rounding in REST and WebSocket feeds.
   - **Gamma Flip Price**: $\le \$1.00$ or $\le 0.005\%$ ($50\text{ ppm}$) of spot price via two-pass coarse/fine discrete spot grid secant interpolation.
   - **Max Pain Price**: Exact strike match ($\Delta = \$0.00$) with deterministic lower-strike tie-breaking against discrete Python reference solver.

2. **Automated Tolerance Test Suite** (`packages/options-engine/src/black-scholes/tolerance.test.ts`):
   - 18 automated tolerance assertions validating all mathematical tiers across parameter matrices.
   - Verified analytical closed-form precision, Call/Put gamma symmetry, Greek display quantization on live snapshots, refined Gamma Flip interpolation bounds, and Max Pain discrete exact matching.

3. **Verification & Quality Gates**:
   - `npm test`: All 35 test files and 164 tests passed with 0 failures (including `tolerance.test.ts`, `dual-engine-parity.test.ts`, `deribit-greek-reconciliation.test.ts`, `golden-options-calculations.test.ts`).
   - `npm run typecheck`: Passed cleanly with zero TypeScript errors across all workspaces.
   - `npm run lint`: Passed with zero errors.
   - Python reference suite (`tools/reference-python`): 59/59 tests passed.
