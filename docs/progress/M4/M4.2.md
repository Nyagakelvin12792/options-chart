# M4.2 Python gamma

Status: COMPLETE
Owner: Antigravity
Updated: 2026-08-26

Evidence: Implemented pure Python reference engine in `tools/reference-python/gamma_reference.py` covering Deribit inverse BTC option gamma (`Gamma = N'(d1) / (spot * iv * sqrt(T))`), modeled signed GEX per 1% spot move (`sign * Gamma * oi_btc * spot^2 * 0.01`), gross gamma concentration (`Gamma * oi_btc` and USD 1% gross exposure), bounded 0.7x to 1.3x spot grid generation, aggregate profile evaluation under sticky IV, and linear interpolation zero-crossing detection with 0.5% significance floor and lower-price tie-breaking. Verified with 19 unit tests in `tools/reference-python/test_gamma_reference.py` including the worked example from `PROJECT_PLAN.md` (S=$100,000, Gamma=0.00002, OI=500 BTC, sign=+1 -> GEX=$1,000,000 per 1% move). All tests passing.
