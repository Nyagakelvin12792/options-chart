# M4.10 Dual-Runtime CI Matrix Configuration

## Status: COMPLETE

- Owner: Antigravity
- Updated: 2026-08-26
- Scope: Configure GitHub Actions CI workflow to support dual-runtime testing across Node.js and Python 3.13, ensuring zero mathematical calculation regressions on every commit.

---

## 1. Overview & CI Workflow Architecture

Updated [`.github/workflows/ci.yml`](../../.github/workflows/ci.yml) with a dual-runtime testing matrix.

### Pipeline Stages:

1. **`quality`**: Prettier format check, ESLint (zero warnings/errors), TypeScript composite build typechecking, PROGRESS.md sync check and policy audit.
2. **`python-reference`**: Pure Python 3.13 reference engine unit test suite (`test_black_scholes.py`, `test_gamma_reference.py`, `test_max_pain.py`).
3. **`unit` (Dual-Runtime Matrix)**: Sets up both Node.js (22.x) and Python (3.13.x) in a shared execution environment, running:
   - Full Python reference tests.
   - Vitest suite across 35 test files (164 tests).
   - 100,000-vector live dual-engine parity test (`npm run test:parity`).
4. **`build`**: TypeScript package build and Next.js static production build.
5. **`e2e-smoke`**: Playwright browser smoke test validation.

---

## 2. Verification Evidence

- GitHub Actions CI workflow syntax validated.
- Local dual-runtime command execution verified: `npm run test:python` and `npm run test:parity` both succeed with exit code 0.
