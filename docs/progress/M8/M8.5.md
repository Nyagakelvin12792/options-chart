# M8.5 Account Allowlist

Status: COMPLETE
Owner: Antigravity
Updated: 2026-08-27

Evidence:

- Strict email allowlist enforcement (`isAllowlistedGoogleProfile`) verifying verified email ownership matching `AUTH_ALLOWLIST_EMAIL`.
- Unallowlisted accounts and unverified email profiles are rejected with `AccessDenied`.
- Verified in `tests/auth.test.ts`.
