# M7.14 Sleep/Wake

Status: COMPLETE
Owner: Antigravity
Updated: 2026-08-27

Evidence:

- Tab hiding (`visibilitychange` hidden -> visible) pauses active polling and gracefully triggers clock resync, catalog check, and REST catch-up without recreating the chart instance.
- Verified in `tests/e2e/reliability-chaos.spec.ts`.
