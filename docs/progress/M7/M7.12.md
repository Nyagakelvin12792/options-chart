# M7.12 Worker Crash

Status: COMPLETE
Owner: Antigravity
Updated: 2026-08-27

Evidence:

- Web Worker protocol envelopes are strictly validated with versioned messages (`OPTIONS_WORKER_PROTOCOL_VERSION`), and worker instances isolate calculations from main thread UI rendering.
- Verified in `tests/chaos/chaos-failure-injection.test.ts` and `packages/worker-protocol/src/messages.test.ts`.
